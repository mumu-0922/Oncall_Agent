global:
  resolve_timeout: 5m

route:
  receiver: aiops-local
  group_by:
    - alertname
    - service
    - instance
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: aiops-local
